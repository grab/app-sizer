/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Gradle plugin project to get you started.
 * For more details take a look at the Writing Custom Plugins chapter in the Gradle
 * User Manual available at https://docs.gradle.org/7.5.1/userguide/custom_plugins.html
 * This project uses @Incubating APIs which are subject to change.
 */

plugins {
    id 'java-gradle-plugin'
    alias(libs.plugins.kotlin.dsl)
}

group = 'com.grab'
version = "SNAPSHOT"

tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).configureEach {
    sourceCompatibility = JavaVersion.VERSION_11
    targetCompatibility = JavaVersion.VERSION_11

    kotlinOptions {
        jvmTarget = '11'
    }
}

dependencies {
    implementation libs.android.gradle.plugin
    implementation libs.gradle.api
}

gradlePlugin {
    // Define the plugin
    plugins {
        sizeAnalysis {
            id = 'com.grab.size-analysis'
            implementationClass = 'com.grab.plugin.size.AppSizePlugin'
            displayName = "SizeAnalysis"
        }
    }
}


def libArtifactId = 'size-analysis'

apply from: '../publish.gradle'
configureArtifact(group, version, libArtifactId)

repositories {
    mavenCentral()
}
